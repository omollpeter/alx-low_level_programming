#ifndef MAIN_HEADER
#define MAIN_HEADER

	#include <unistd.h>
	#include <limits.h>
	#include <stdio.h>

	/* Function prototypes */
	int _putchar(char c);
	int _isupper(int c);
	int _isdigit(int c);
	int mul(int a, int b);
	void print_numbers(void);

#endif
