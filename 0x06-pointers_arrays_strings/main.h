#ifndef MAIN_H
#define MAIN_H

	#include <stdio.h>
	#include <unistd.h>
	#include <string.h>

	/* Function prototypes */
	int _putchar(char c);
	void reverse_array(int *a, int n);
	char *_strcat(char *dest, char *src);

#endif /* MAIN_H */
